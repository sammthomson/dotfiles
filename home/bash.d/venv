export WORKON_HOME="/opt/virtualenvs"

for venvdir in "/usr/local/bin" "/Library/Frameworks/Python.framework/Versions/2.7/bin"; do
    if [ -e ${venvdir}/virtualenvwrapper.sh ]; then
        source "${venvdir}/virtualenvwrapper.sh"
    fi
done

alias ipy="python $(which ipython)"
